name: 'dbt_handson'
version: '1.0.0'
config-version: 2

profile: 'dbt_handson'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_handson:
    staging:
      +materialized: view
    marts:
      +materialized: table
